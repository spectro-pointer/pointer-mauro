- General
    Fix Z stuck
        better construction(Gustavo)
        grease
        right torque
    control
       	pointAzEl()
           fix small movements when zero offset

    fix pointer_cli help

- client-server:
	zero at server start
		save our absolute position at exit
	
    dir change auto adjust
    	based on webcam image movement
    
    buffered(mq)
    	see pyrit's enqueue
        priority/cancel(mq bypass/erase)
    
    usb webcam
    	compass
    	level    	

    	target tracking

    config
        server-side config file
        	see pyrit's config
        	save pos/settings at exit

        set change dir adjust offsets
        set relative to zero offsets
        	True North(Including magnetic declination)
        	0º Elevation
        	
        set speed/ on/off delay

        auto calibrate
        	dir change offsets
        		using webcam
            max speed
                using webcam/switches
        ...
        
Functions:
	- Telescope pointing
		RA/Dec settings
			set lat/long
			conversion to Az/El
		Earth rotation compensation
			ntp time
	- Target tracking
		moving target detection
			webcam based
		brightness/color tracking
			max brightness tracking
			specific color(green laser?) tracking
	- Window monitoring/sweeping
		custom window
		custom speeds
		
GUI:
	Settings
	Telescope pointing
	Window sweeping
	Moving target tracking
		webcam based
	
Raspberry Pi
	GPIO EightIO driver
	webcam driver
